/**
* Method is used for inserting in Sorted Linked List
* Assumption : List is already sorted element.
**/
public void sortedLinkedList(Node head, int ele) {
   Node newNode = new Node(ele);
   Node temp = head;
   while(tem) {
   
   }
}
